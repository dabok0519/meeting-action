"""OpenRouter 연결 테스트 스크립트. python test_openrouter.py 로 실행."""
import json

from openrouter_client import call_openrouter

SAMPLE_MEETING_TEXT = """
일시: 2026년 7월 11일
참석자: 박팀장, 김인턴, 이대리

[회의 내용]
회의록을 입력하면 AI가 액션 아이템을 자동으로 구조화해주는 웹서비스의 MVP(최소 기능 제품) 개발 방향을 논의했다.

먼저 UI 디자인과 프론트엔드 개발 방향에 대해 이야기했다. 이대리는 "사용자가 회의록을 붙여넣었을 때 로딩 상태를 직관적으로 보여주는 디자인이 필수적이다"라고 강조했다. 이에 따라 김인턴이 다음 주 화요일(7월 14일)까지 React를 활용해 로딩 스피너가 포함된 메인 UI 프로토타입을 개발하기로 결정했다.

백엔드와 DB 설계에 대한 논의도 이어졌다. 박팀장은 "인턴 과제 특성상 복잡한 RDS 설정보다는 빠르게 뼈대를 잡는 것이 중요하다"라며, 초기 DB는 로컬 SQLite를 사용해 1:N 관계형 모델로 빠르게 구축하라고 지시했다. 이 부분은 이대리가 주도하여 7월 13일(월요일)까지 FastAPI 엔드포인트와 DB 스키마 설계를 완료하기로 했다.

마지막으로 AWS 배포 환경에 대해 토론했다. 프리티어 범위 내에서 안정적으로 구동하기 위해 Docker를 쓸지 EC2에 직접 올릴지 고민했으나, 이번에는 가장 기본적이고 직관적인 EC2 인스턴스에 직접 배포하는 방식을 채택하기로 최종 합의했다. 배포 과정에서 막히는 부분이 많을 것으로 예상되므로, 김인턴이 배포 과정을 꼼꼼히 트러블슈팅 문서로 정리하여 7월 15일(화요일) 오후 2시까지 공유하기로 했다.
"""

if __name__ == "__main__": # 현재 py에서 실행할때만 동작하도록 설정 
    result = call_openrouter(SAMPLE_MEETING_TEXT)
    print(json.dumps(result, ensure_ascii=False, indent=2)) # 들여쓰기 2칸, 한글 깨짐 방지
